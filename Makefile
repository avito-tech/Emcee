open: 
	bash make.sh open

clean:
	bash make.sh clean

build: 
	bash make.sh build

test:
	bash make.sh test

test-parallel:
	bash make.sh test-parallel

integration-test:
	bash make.sh integration-test
